import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 0,
        margin: 0,
        backgroundColor: "#fff"
    },
    modalContent: {
        backgroundColor: "#fff"
    }
})